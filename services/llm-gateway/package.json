{
  "name": "@qunta/llm-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-hidden LLM gateway for Qunta.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "test": "node test/gateway.test.mjs && tsx test/usage-meter.test.ts && tsx test/rate-limit.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@qunta/shared-types": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
