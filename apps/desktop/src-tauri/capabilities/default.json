{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default deny-by-default permissions for the Qunta shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
