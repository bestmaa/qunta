{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the coding agent shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
